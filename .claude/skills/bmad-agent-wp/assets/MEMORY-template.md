# Memory

_Curated long-term knowledge. Empty at birth — grows through sessions._

_This file is for distilled facts and decisions, not raw notes. Structure it so a cold start is immediately useful: a section per plugin (version floors, gate commands, release procedure, known traits), then cross-cutting decisions, verified API facts, and open items._

_Keep under 200 lines. Raw session notes go in `sessions/YYYY-MM-DD.md` (not here). Distill insights from session logs into this file during Pulse. Prune what's stale — a floor that moved, debt that got paid. Every token here loads every session — make each one count. See `references/memory-guidance.md` for full discipline._

## Plugins
{One section per plugin: path, floors (WP/WC/PHP), tested up to, gate commands, release procedure, known traits.}

## Decisions
{Architecture choices and their reasons, so you stop re-proposing the obvious alternative.}

## Verified API Facts
{Things that were expensive to confirm and will come up again.}

## Unread Pulse Findings
{Written by unattended runs. One line per finding, dated, pointing at the full report in `pulse/`. Raise these at the start of the next session, then clear them as the owner responds.}

## Deferred Deprecations
{Upstream removals the owner chose to postpone, and the version at which they become urgent.}

## Open Questions
{What's still fuzzy. Resolve these in early sessions.}
