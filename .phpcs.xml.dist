<?xml version="1.0"?>
<ruleset name="Skwirrel PIM sync for WooCommerce">
    <description>Code style rules for Skwirrel PIM sync for WooCommerce plugin.</description>

    <!-- Scan these files -->
    <file>plugin/skwirrel-pim-sync/skwirrel-pim-sync.php</file>
    <file>plugin/skwirrel-pim-sync/includes/</file>

    <!-- Exclude vendor -->
    <exclude-pattern>vendor/</exclude-pattern>
    <exclude-pattern>plugin/skwirrel-pim-sync/vendor/</exclude-pattern>
    <exclude-pattern>tests/</exclude-pattern>
    <exclude-pattern>node_modules/</exclude-pattern>

    <!-- Use WordPress Extra standard (includes Core + Docs subset) -->
    <rule ref="WordPress-Extra">
        <!-- Allow short array syntax [] instead of array() -->
        <exclude name="Universal.Arrays.DisallowShortArraySyntax"/>
    </rule>

    <!-- Check text domain -->
    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array">
                <element value="skwirrel-pim-sync"/>
            </property>
        </properties>
    </rule>

    <!-- Minimum supported WordPress version -->
    <config name="minimum_wp_version" value="6.0"/>

    <!-- PHP compatibility -->
    <arg name="extensions" value="php"/>
    <arg value="sp"/>
    <arg name="colors"/>
</ruleset>
