# PRD Decision Log — Skwirrel PIM sync for WooCommerce

Canonical memory + audit trail for this PRD run. Every decision, change, and override is recorded here.

## Run metadata
- **Intent:** Create (no prior PRD existed)
- **Date:** 2026-06-10
- **Facilitator mode:** Fast path (user opted in)
- **Workspace:** `_bmad-output/planning-artifacts/prds/prd-wordpress-2026-06-10/`
- **Stakes:** Launch-level (commercial, WordPress.org-distributed plugin), but scoped to one maturity chapter.

## Discovery decisions

1. **PRD is forward-looking, not retro-documentation.** The plugin works well today (v3.10.2); this PRD defines the *next chapter* — a quality/maturity theme, not a new-feature land-grab.
2. **Core problem named by user:** "the plugin as is is nice, but becomes complex … simple to use and more in control." Complexity is accumulating as the product grows.
3. **Sharpest pain = admin UX:** "all kinds of settings and relations … really hard for customers to use." Logging exists ("we log things") but "could be better integrated."
4. **The defining problem = fault isolation:** "people have issues, but is it our plugin or the whole stuff with other plugins." With WP 7.0 out, the "is it us or the environment?" question is the heart of the PRD.
5. **Audience = non-technical:** "store owners, marketeers, people that don't want to invest in a good webshop." Drives the simplicity imperative.
6. **Diagnostics goal = customer self-service:** "customer self service is always the goal."
7. **Success definition (user, verbatim):** "updates in wp wont harm this. complete control and fast syncs." → three success dimensions: (a) resilience to WP/WC/ecosystem updates, (b) customer sense of complete control, (c) fast syncs (performance is a first-class success measure).
8. **Horizon = WP 7.0 era.** Concrete timing anchor; pairs with the 7.0+ primary / 6.9+ floor compatibility posture already set in `project-context.md` and v3.10.2.

## Three pillars of the PRD (synthesized + confirmed in spirit)
- **Pillar A — Tame configuration complexity** (simpler, legible admin UX; sensible defaults; guided setup).
- **Pillar B — Integrated diagnostics & fault isolation** (self-service "is it us or the stack?" answers; health, conflict/compat detection, environment report, surfaced sync state).
- **Pillar C — WP 7.0-era resilience on a well-tested, safe-update, fast-sync backbone** (NFR backbone made first-class).

## Open assumptions to confirm (Fast path `[ASSUMPTION]` tags — see PRD §9)
- Q4 (non-goals) not explicitly answered → assumed: this chapter does NOT add new PIM/sync data features; scoped to simplicity + diagnostics + resilience. **Needs confirmation.**
- Quantitative success targets inferred (ticket reduction %, sync time targets). **Needs confirmation.**
- "Complete control" interpreted as customer-facing visibility/preview/safety, not team release tooling. **Needs confirmation.**

## Workflow sequencing (decided 2026-06-10)
- **Order is: finish PRD → Architecture → UX.** Architecture comes *after* the PRD is finalized (user decision, 2026-06-10). The hybrid sync rewrite (fixes F4/F6/F7 from the WP 7.0 investigation — per-product-atomic + thin relations pass) is the headline input to that Architecture step.
- **Out-of-band now:** the 3.10.3 recovery release (F2 scheduler re-arm + F1 connector type + F3 category rename) ships immediately via quick-dev, independent of the PRD/Architecture track.

## Changes after draft
- **2026-06-10 — WP 7.0 root-cause evidence (production log).** User supplied a real sync log (run 2026-06-08). It complicates the Connectors hypothesis: the run *succeeded* (authenticated, updated 1,156 products), so Connectors/token is NOT always the failure. The log surfaced a concrete WC-API break: `Call to undefined method ...ApprovedDirectories\Register::is_approved_directory()` (WARNING during downloads auto-approve) — a removed/renamed WC core method the plugin still calls. Now ≥2 candidate 7.0 failure modes (Connectors token path; WC ApprovedDirectories API), likely more. FR-13 §4.0 Notes updated; investigation should sweep all WC/WP core methods the plugin calls, with a *failing* run log as primary input. Also captured a perf baseline: ~1,156 products ≈ 12 min full run → §10.
- **2026-06-10 — Priority 0 reframe.** User: "first goal is make it back to work. since 7.0 of wp the plugin has issues." Elevated WP 7.0 *restoration* above the maturity chapter. Added §4.0 (FR-13 restore 7.0 operation, FR-14 regression coverage), SM-0 gating metric, Priority-0 callout in §0, reprioritized §6.1 MVP, updated §11 Why Now. The maturity pillars (A simplicity, B diagnostics, C resilience) now follow the fix. **Critical open input:** the concrete 7.0 symptoms are not yet enumerated (Open Question 7) — single most important missing fact before Architecture/Dev.
- **2026-06-11 — §4.0 Priority-0 RESOLVED & shipped (v3.10.2). Open Question 7 answered.** The WP 7.0 break was a *cluster* of independent WP/WC-7.0 changes, not the hypothesized single Connectors-token fault (that hypothesis was **refuted** by a successful production run). Root causes found, fixed, and verified on real clean-DB full sync (1,321 created/0 failed) + delta sync (1,199 updated/0 failed): **(F2)** scheduled syncs silently stopped after auto-update — schedule armed only on activation, which auto-updates skip → re-arm on version change + admin-load self-heal; **(F1)** Connectors `_doing_it_wrong` notice → `type=service`; **(F3)** category renames/moves not propagating → reconcile matched term via `wp_update_term()`; **(documents)** all downloadable files rejected because the approved-dir pre-check called the removed WC `is_approved_directory()` → use `is_valid_path()`. Plus connectors-link + branded logo. Shipped: commit `81ec147`, tag `3.10.2`, GH release + WP.org deploy triggered. PRD updated: §0 callout, §4.0 (status + actual root causes), FR-13 (MET), FR-14 (PARTIAL — unit tests for scheduler+category landed; **documents-path 7.0 integration test still to automate**), SM-0 (MET), Open Question 7 (answered), §9 (gap resolved), §11 (Why-Now reframed: recovery *proved* the Pillar-B diagnostics need). **Net effect:** the PRD's single highest-priority gap is closed; forward work is now Pillars A/B/C, and the F4/F6/F7 hybrid-sync rewrite remains the headline Architecture-step input (unchanged).
- **2026-06-11 — Four finalization decisions (user answers).** (1) **Control scope EXPANDED** beyond visibility: user wants **preview-before-sync** *and* sync-result counts (added/changed/removed) that are **clickable, deep-linking to a filtered WooCommerce product view** showing exactly those products. → Added **FR-15** (clickable result→product deep-links, needs a per-run identity marker; low-cost, ships with FR-8) and **FR-16** (preview/dry-run, read-only, count+removal-list level — *not* per-field diff in v1; heaviest item, designed to share the resolution path with the planned hybrid sync-core rewrite). §4.2 retitled "Diagnostics, Fault Isolation & Sync Transparency"; both added to MVP (FR-16 flagged as the swing item to confirm MVP vs fast-follow once Architecture sizes it). Open Question 6 answered. (2) **Audience = secondary, don't block** — §2.2 confirmed, Open Question 1 closed. (3) **Speed = delta-first** — no full-sync time budget this chapter; the only enforced guarantee is delta touches only changed products (zero when nothing changed). §4.4/FR-12/SM-3/§10 reframed; full-sync tail (attribute-merge, relations, redundant per-variation writes) recorded as known debt for the Architecture rewrite, not a chapter target. Open Question 3 closed. (4) **Locales = English-first**, translatable day one, full coverage may trail — §6.2 confirmed, Open Question 5 closed. **Remaining open (data-dependent, not Architecture blockers):** SM-1/SM-2/SM-5 quantitative targets + support baseline (Q2); top customer Conflicts to seed FR-6 (Q4); confirm FR-16 preview depth (count/list vs richer) and the §5 no-new-data-features boundary. **Status → ready for the Architecture step** (PRD→Architecture→UX sequence holds).
- **2026-06-11 — Jeroen's asks added, then REVERTED (wrong project).** Briefly added FR-17 (feature health checklist), FR-18 (add-on registered/active overview), FR-19 (add-on activation papertrail) from a relayed Jeroen conversation. **Correction (same day):** these are features of **Skwirrel core (the PIM platform)** — a *different product* — not the WooCommerce connector plugin this PRD covers. **Backed out in full:** FR-17/18/19 removed, glossary entries (Feature Checklist, Add-on, Add-on Activation Log) removed, §4.2 description and §6.1 MVP line reverted. PRD FR set returns to FR-1–FR-16. **Lesson:** scope-test stakeholder asks against *this* product before writing FRs. *(If a future connector-side want emerges to surface Skwirrel-core health/add-on status inside the WP plugin, that would be a new, separate FR — not assumed here.)*
- **2026-06-11 — FR-15/FR-16 confirmed ("that is success").** User re-raised both wants (sync result table → filter to WooCommerce product list; preflight/preview of what will be synced) and confirmed they are correctly captured as FR-15 (clickable result→product deep-links) and FR-16. FR-16 renamed to use the user's term **"preflight"** (preflight / preview / dry-run) for findability. These are the locked "complete control" capabilities.
- **2026-06-11 — Sync SPEED deferred to a later chapter (was delta-first).** User: "speed of syncs come later." Superseded the earlier delta-first decision: sync performance (full *and* delta wall-clock) is now **fully out of scope** for this chapter, not just full-sync. §4.4 retitled "Sync Performance — Deferred"; FR-12 reduced to a pure **delta correctness** regression guard (no speed target); SM-3 reframed to correctness-only; §5 Non-Goals adds the speed deferral; §10 perf posture marked deferred (baseline ~8–12 min kept as known debt); §1 vision softened; SM-C1 reframed as a standing correctness guard that pre-binds the future speed work. The ~12-min full-sync tail (attribute-merge, relations, redundant per-variation writes) is parked for the later perf chapter / hybrid-rewrite. **Net:** this chapter = simplicity + diagnostics + resilience + control (preflight/deep-links); speed explicitly later.
- **2026-06-11 — Walkthrough of remaining opens + 1 new FR (FR-17) + customer evidence.** (Q1 support data) **No formal ticket tracking** → SM-1/2/5 stay provisional; action = start tagging tickets; not a blocker. (Q2 FR-6 conflict seeds) confirmed: **image/media optimizers + caching/performance + (maybe) permalinks**. The other two items the user listed ("delete all", "renaming categories") were **reclassified out of FR-6** — they are the plugin's own category/product *propagation* behavior, not third-party conflicts. (Q3 FR-16 depth) **LOCKED: count + list** (which products, esp. removals; no per-field diff). (Q4 §5 boundary) **CONFIRMED**: no new PIM/catalog data; FR-15/FR-17 operational data is inside the boundary. (Q5 add-on def) moot — Jeroen items already removed as wrong-project.
  **New FR-17 — opt-in "Start over / Clean all" reset.** User clarified "delete all is now only Skwirrel-related" (deletion already correctly scoped to plugin-owned products) and asked for an explicit **opt-in** "start over / clean all" control to wipe the Skwirrel catalog and re-sync fresh. Added as FR-17 in §4.2: Skwirrel-scoped only, never automatic, confirmed + preflight-previewed before run, builds on the 3.9.1 reset escape-hatch. Open: Trash (recommended) vs permanent delete. §4.2 description reframed around a sharper through-line — **make structural/destructive operations (rename, delete, mass change, reset) legible and safe**, since that's where customer fear and "is it us?" confusion live.
  **Customer evidence captured (one-customer context).** Real case: a category rename in Skwirrel propagated to products only after **too-long latency** with the category-level change unclear; and a deleted category subtree (Webshop→Radiocom + submenus) was being watched for propagation. Diagnosis: rename propagation was *broken pre-3.10.2* (F3 fixed it via `wp_update_term()` reconciliation — customer may not be on 3.10.2 yet); stale-category **deletion** only removes terms on a **full sync without collection filter** and **only when no products are still attached** (safety rule) — which explains the lag. **Strong motivation for FR-16 preflight (esp. category-tree deletes).** Open follow-up offered to user (A: PRD evidence only / B: focused category-propagation verify-or-fix like 3.10.2 / C: both) — pending.
- **2026-06-11 — FR-16 (and FR-15) extended to the CATEGORY-STRUCTURE level. User: "this is really a good thing. Give full insights and control."** Rationale: the customer evidence shows the fear and "is it us?" confusion live at the **category-tree** level (renaming a category, deleting Radiocom + submenus), not the product level. A preflight that reports "12 products will change" but is silent on "1 category + 6 subcategories removed, 240 products re-homed" misses the scary part. **FR-16** now forecasts both levels: products (add/change/remove) **and** category structure (created / renamed old→new / removed-or-orphaned, with the count of products a removal/rename would re-home/detach; whole-subtree deletes shown as parent+descendants). **FR-15** mirrored: the *result* surfaces category-structure changes too, so before (preflight) and after (result) speak the same product-and-category language. Still read-only (preview mutates nothing). §4.2 description reframed around the through-line "make structural/destructive operations legible and safe." Added **UJ-4** (Pieter reorganizes the category tree without fear — real-customer-derived) tying FR-16 + FR-15 + FR-17 + FR-8. Per-field diff remains out of scope for v1.
