# 2026-09-14
- Jos asked: can ETIM sync be turned off? Verified: NO setting exists. ETIM is hardcoded on — `include_etim`/`include_etim_translations` = true in service.php:315-316, 1934, 2120, 2251, 2393 and upserter.php:1022; `Product_Mapper::get_attributes()` (:802) always merges ETIM attrs, ungated. Unlike custom classes (`sync_custom_classes`, `sync_trade_item_custom_classes`). Only ETIM mention for users: readme.txt:21. Grouped products need ETIM for variation axes — any toggle must keep that path.
- Built `sync_etim` (default on) on release/4.0.0, uncommitted, mixed into Jos's existing WIP files. `Service::needs_etim_payload()` = sync_etim || sync_grouped_products → include_etim on main page fetch (:315) + run_single_product. Grouped single-sync fetches left true. Mapper::get_attributes() skips ETIM when off. Checkbox in Dashboard::render_fieldgroup_sync_options + hint. Test: tests/Unit/SyncEtimSettingTest.php. Verified: variation axis values come from member `_etim` (upserter.php:743), NOT only from getGroupedProducts.
- API doc (Jos screenshot): include_etim default false; include_etim_translations default TRUE, requires include_languages; include_etim_releases ?string[].
- POT regen also exposed Jos's WIP rename "API Token"→"API token" (dashboard.php:1491) which failed TranslationCompletenessTest; filled from old translation.
- Not fixed, flagged: simple product with zero attrs after toggle keeps stale ETIM attrs (upserter.php:566 only set_attributes when non-empty). Version not bumped — CHANGELOG under [Unreleased]; /release owns bump + readme entry.
- add-setting skill is stale: says Dutch UI + old filenames (class-admin-settings.php); form now lives in admin-dashboard.php.
- Stale-attr fix: `Product_Upserter::clear_stale_attributes()` (simple only) on empty attrs in upsert_product + assign_attributes. Verified in WC source: `WC_Product::set_attributes()` nulls omitted keys → data store `update_attributes()` unsets their terms. Integration test SyncEtimAttributesIntegrationTest (fails without fix). Variable-parent stale non-variation attrs (flush_parent_attribute_terms merges only) NOT fixed — awaiting Jos.
- 4.0.0 prepared, NOT committed/tagged (Jos: "do not tag and release yet"). Bumped header, constant, Stable tag, package.json, package-lock (x2). CHANGELOG [4.0.0] + readme = 4.0.0 =. Gates green: pest 686, phpstan OK, phpcs clean; integration 240 passed twice in a row.
- SettingsTabsIntegrationTest pins every settings input name (skwSettingsBaselineInputNames) — any new setting must be added there.
- Integration suite showed flaky cross-file failures (ContextId, DocumentNames, NonDestructiveMapping…) on some full runs after filtered/mutation runs; each file passed alone, and two consecutive full runs later were green. Cause unverified.
- Sync-completed poller message (Admin_Settings::enqueue_status_banner_assets $completed_html) switched to core `notice notice-success inline`; old .skw-progress-done card CSS removed from dashboard.css. Same msgids, no i18n change. Gates green. Not visually verified in browser.
- Committed 8f85600 "Release 4.0.0" on release/4.0.0 (all tree changes incl. a parallel session's integration-test/dashboard edits). NOT tagged, NOT pushed. Gates + integration (240) green at commit.
